# Project for intro to data science classes (Vistula University 2026/27)

## Installation & Setup
The relevant dependencies and virtual environment can be initialized with either uv or pip.

### uv
```bash
uv sync
```
### pip
```bash
python -m venv .venv
source .venv/bin/activate
pip install .
```